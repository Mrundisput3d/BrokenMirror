# Copy to .env (gitignored) and fill in. Run the bot with: npm run bot
DISCORD_TOKEN=
GEMINI_API_KEY=
# Optional — defaults to serviceAccount.json if unset
GOOGLE_APPLICATION_CREDENTIALS=serviceAccount.json

# Secrets — never commit the Firebase service account key
serviceAccountKey.json
serviceAccount.json
*serviceAccount*.json
.env

# Node
node_modules/

# Local scrape snapshots (--out artifacts); the site reads from Firebase
regroups.json

# Local IDE
.idea/

# Temporary local hourly-updater loop (PC only; the VPS uses a systemd timer)
run-respawn-loop.ps1
respawn-loop.pid
scraper.log
